# 更新记录

## 1.4.3

- 保存并回填邮箱和密码，密码通过 Android Keystore 加密；兼容旧版密码摘要。
- 新增自定义刷新 Token：自动填入登录获取的令牌，支持仅 Token 登录；手动令牌失效时提示更新。
- 新增香港与 Cloudflare 线路选择，切换时重建会话并保留登录配置。
- 适配官方 Web 最新漫画接口：用 `GetBooksBySeries(Type=Comic)` 和 `GetBookInfo` 替代已移除的 `GetComicSeriesInfo`，保留原有系列地址和多上传版本章节。

- 缓存访问令牌：JWT 在过期前刷新；无可读有效期的令牌按官方 Web 的 30 秒窗口缓存。
- 连续漫画请求复用长轮询会话，使用独立调用 ID，空闲 30 秒后关闭；并发请求共用认证和会话。
- 明确认证失效时刷新并重试一次；临时 502/503/504 有限重试，保留刷新令牌。
- HTTP 403/404 不再直接被判定为刷新凭据失效；修改账号时清除内存缓存并异步关闭连接，避免在设置界面发出阻塞网络请求。

## 1.4.2

- 修复 Mihon 0.20.4 打开热门漫画列表时的 `LinkageError`：改用 SignalR HTTP 长轮询，移除对宿主 WebSocketListener 回调的继承。
- 保留漫画数据接口、源 ID 和登录配置；增加长轮询认证、连接清理及宿主 WebSocket ABI 的回归测试。

## 1.4.1

- 初始化漫画扩展，支持热门、最新、搜索、详情、章节、图片分页与账号登录。
